{
  "_comment": "Pack component name to Figma component key. Generated by scripts/sync-figma-keys.mjs from the published library, and committed so a build never needs a live design connection. Empty until the sample file exists: until then the bridge reports every instance in a frame as unmapped, which is the truth rather than a guess.",
  "file_key": null,
  "components": {}
}
