/**
 * Stylesheets are imported for their side effect. There is nothing to name, because a component
 * gets its look from the tokens the pack compiled, not from a generated class map.
 */
declare module '*.css'
