{
  "name": "figma-bridge-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "test:types": "tsc --noEmit",
    "test:all": "pnpm test:types && pnpm test",
    "bench": "esbuild src/figma.ts src/issue.ts --bundle --format=esm --platform=neutral --outdir=.bench --log-level=error && node bench/cpu.mjs"
  },
  "dependencies": {
    "jose": "^5.9.6"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.5.0",
    "@cloudflare/workers-types": "^4.20240909.0",
    "esbuild": "^0.28.2",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0",
    "wrangler": "^4.135.0"
  }
}
